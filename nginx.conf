server {
  listen 80;
  root /usr/share/nginx/html;

  gzip on;
  # .gltf has no mime type entry so it lands in octet-stream; gzip it anyway (they are JSON text)
  gzip_types application/javascript text/css application/json application/octet-stream;
}
