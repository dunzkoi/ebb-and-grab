server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # .gltf has no default mime type; without this it is octet-stream and never gzipped
  types { application/json gltf; }

  gzip on;
  gzip_min_length 1024;
  gzip_types application/javascript text/css application/json;
}
